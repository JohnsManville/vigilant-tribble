# STATUS ROLLUP — 2026-08-02 (~01:00)
**All code in flight, roughly 7/26 → 8/2.** Written by the MacBook Claude session as the compaction
of every open session + handoff doc. This file supersedes the per-session state; the July sessions
are fully captured here and safe to archive.

**File reorg (tonight):** all loose handoff/status docs moved from `~/Documents` into
`~/ClaudeBox/handoffs/` (superseded ones in `handoffs/archive/`). This folder now rides the nightly
rsync to the Mini automatically (it's inside ClaudeBox, not excluded). A one-off sync to the Mini was
run tonight. `~/Documents/STATUS-LATEST.md` is a symlink to the current rollup.

---

## 1. Infra / ClaudeBox

- **Mini reboot: STILL PENDING — blocked on Jeff's password.** This morning's session found a second
  user (`karifloof`, console, logged in since Jul 31) blocks every passwordless restart path; the
  graceful restart self-cancelled. Mini is UP and healthy (SSH over Tailscale verified tonight).
  Fix is one of:
  - `ssh -t -i ~/.ssh/whipdesk_mini_ed25519 jeffmumfordmacmini@100.115.69.54 'sudo shutdown -r now'` (type Mini password), **or**
  - log out the `karifloof` session via Screen Sharing, then any session can re-send the no-password restart.
  - Reboot kills the 4 Claude CLI sessions on the Mini (resumable via `claude --resume`).
- **Nightly sync: HEALTHY.** 8/1 run: all 5 repos in sync (finrecon ff'd 4 commits), rsync both ways clean.
- Still open from the 7/29 remote-access handoff (Jeff's clicks): **AnyDesk unattended passwords on
  both machines** (kills the manual-accept) · Termius key import on iPhone · pick a Mini-down alert
  method (heartbeat can't report its own death).

## 2. DGUTS '26 (raft trip — campingteam.us)

- Site LIVE with Press page; RSVP form fixed & verified; jobs Worker `dguts-jobs` DEPLOYED and
  end-to-end tested (api.campingteam.us).
- **ONE code action remains: redeploy the site folder** so the live crew page picks up the job board.
  Verified tonight: live crew.html has **zero** references to api.campingteam.us — still the old
  static version. Files staged at MBP `~/Downloads/campingteam-site/` (worker source in
  `~/Downloads/dguts-jobs-worker/`). Wrangler is authenticated on the Mini (that's how the 8/1
  deploy went out). **Scheduled for tomorrow 9:00am.**
- Jeff's hand: send "The Deschutes Dispatch" email (built, email-client-safe, never sent — no-sends
  standing order). RSVP deadline on everything = **Aug 7**, trip Aug 12–16.
- Full context: `handoffs/HANDOFF-DGUTS26-SESSION-IMPORT.md`.

## 3. Handled (Django job-to-cash app)

- **`nightly` merged to `main` @ `a978f73`, 155 tests green** (template fixes: OOB swaps, proposal
  dark-mode). Customer/property read views landed earlier in the week. Notion command-center context
  committed as CLAUDE.md @ `6f9fd80`. The 2am crew was RETIRED 8/1 (plist archived).
- All clones clean, no uncommitted work. MBP's spare `handoff-clone` is a few commits behind — the
  nightly sync will fast-forward it.
- Next: **Square sandbox → go-live** (blocked on Jeff) · e-sign legal gate · the never-queued
  "proposal → invoice record" build (Claude-runnable any time).

## 4. Notion sub-scheduling system — **COMPLETE 8/1**

- Roster = 12 subs, both TEST records trashed, Pay $60/Per Job set on the Girgich screen-door
  assignment. Notion MCP OAuth verified on this account — future sessions have live access.
- Nothing open. Parked ideas (calendar sync, automated offers) stay parked; any send automation runs
  through the no-sends order. Reference: `handoffs/SUBCONTRACTOR-TOOL-RETRIEVAL.md`.

## 5. FinRecon / Schedule C

- **v1.30 landed** (crew roster, casualty lodging, Venmo/cleaner, card-1009, Amazon parse) on top of
  the week's v1.28–v1.29 sweeps. Repo clean.
- Open = mostly Jeff's docket: 4 mystery deposits · 19-item "Needs your input" list · USAA
  sign-in-and-drive session for the 6 deposits gating the 2024 packet · Amazon Order History export
  (gates ~$21–26k) · escrow-check recording after the branch deposit.
- Claude-runnable next: QuickBooks invoice export → Amazon matching · `docs verify` sweep ·
  render "PROJECT RECORD AND ACCOUNTING — 22 July" → PDF (file now in `handoffs/`) · fix the
  GPT-bridge `OPENAI_API_KEY` env var.

## 6. Mumford Week Plan

- No code movement this week; prod at `97fdeed`. **Stocked only through TODAY (Sun 8/2) — the first
  unpublished week hits tomorrow.** **Scheduled for tomorrow 8:30am:** publish the week of 8/3 per
  existing cadence, stress-test first (standing law), and take a crack at reproducing **Laura's
  changes/decisions-tab bug** (reported 7/23, never picked up).
- Still waiting on Laura re the pre-count-option-overnights proposal. Tracker redesign remains
  forbidden without new instruction.

## 7. Purcell matter

- On hold pending attorney. **Settlement deadline Aug 10 (8 days out); payment deadline Aug 20;
  escrow check voids ~Aug 26.**
- Jeff's hands, unchanged: send the attorney email (draft complete, needs To: + Send) · deposit the
  **$8,837.38 escrow check at a Chase branch** · the people/vendor asks.
- Claude-runnable, queued but NOT scheduled (Mini-side, say the word): permit lookup · re-run
  `purcell-hidden-costs-2` then `purcell-full-accounting` · regenerate the Counsel Packet +
  Adversarial Analysis (three named defects). Standing law: draft anything, transmit nothing.

## 8. Security (unchanged, all Jeff)

1. Rotate the 3 exposed API keys (2 in the Mini's Notes, 1 in a Jul-20 transcript on both machines).
2. Set the Anthropic console spend cap.
3. AnyDesk unattended passwords (also §1).

## 9. Bitsies / Us game / leads (no movement this week)

- Bitsies: live in prod; blocked on the missing factory-doc assets (Jeff/John).
- Us game: Kari's new link (HXCYAM) still unsent; keep/kill marks owed.
- Hot leads (from 7/25 distill, re-verify freshness): Larysa Gosney ASAP · Hinckley wants a call ·
  Girgich ready · the 808 number unanswered since 7/20 · helper has asked for hours 3×.

---

## Scheduled runs created tonight (one-time, tomorrow Sun 8/3)

| Time | Task | What it does |
|---|---|---|
| 8:00a | `morning-infra-check-2026-08-03` | Mini reboot status (nag with the one-liner if still pending), nightly-sync log both machines, handoffs-folder sync verify, heartbeat check |
| 8:30a | `week-plan-publish-2026-08-03` | Publish week of 8/3 (stress test → publish → verify), attempt repro of Laura's changes-tab bug |
| 9:00a | `dguts-site-redeploy-2026-08-03` | Push `campingteam-site/` live via the Mini's wrangler, verify the job board end-to-end |

Note: local scheduled tasks fire while the Claude app is open on the MBP (or on next launch).
